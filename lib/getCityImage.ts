/**
 * ⚠️ 이 파일은 **재수출만** 합니다. 내용은 `shared/city-images.ts` 에 있습니다.
 *
 * 웹과 앱이 똑같이 쓰는 값이라 한 곳(shared/)에만 두고 앱은 복사해 갑니다.
 * 고칠 일이 있으면 **shared/ 를 고치세요.** 여기를 고치면 앱과 어긋납니다.
 */
export * from "@/shared/city-images"
